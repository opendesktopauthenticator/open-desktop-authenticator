server {
	listen 80 default_server;
	listen [::]:80 default_server;
	server_name _;
	# 444: close the connection without a response. A scanner learns nothing,
	# not even that something is listening and refusing.
	return 444;
}
